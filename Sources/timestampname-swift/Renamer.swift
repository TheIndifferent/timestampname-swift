import Foundation

func prepareRenameOperations(items: Array<FileMetadata>, noPrefix: Bool) -> Array<RenameOperation> {
    var operations = [RenameOperation]()
    // TODO implement the mapping
    return operations
}
